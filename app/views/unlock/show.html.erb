<h1><%= @title %></h1>
<% @paragraphs.each do |paragraph| %>
  <p><%= paragraph %></p>
<% end %>
<% if !@links.empty? then %>
  <ul>
    <% @links.each do |link| %>
      <li><a href="<%= link['url'] %>"><%= link['text'] %></a></li>
    <% end %>
  </ul>
<% end %>
  
